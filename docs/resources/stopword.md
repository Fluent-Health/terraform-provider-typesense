---
page_title: "typesense_stopword Resource - typesense"
subcategory: "Search Tuning"
description: |-
  A stopwords set is a named list of common words removed from search queries that reference this set via the stopwords search parameter. See the Typesense API docs https://typesense.org/docs/30.2/api/stopwords.html.
---

# typesense_stopword (Resource)

A stopwords set is a named list of common words removed from search queries that reference this set via the `stopwords` search parameter. See the [Typesense API docs](https://typesense.org/docs/30.2/api/stopwords.html).

## Example Usage

```terraform
resource "typesense_stopword" "common_en" {
  name      = "common_en"
  locale    = "en"
  stopwords = ["the", "a", "an", "of", "and"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String)
- `stopwords` (Set of String)

### Optional

- `locale` (String) Locale of the stopwords (e.g. `en`, `de`).

### Read-Only

- `id` (String) The ID of this resource.

## Import

Import is supported using the following syntax:

```shell
terraform import typesense_stopword.common_en common_en
```
